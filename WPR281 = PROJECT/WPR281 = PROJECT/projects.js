// Member 1 scope note:
// This page UI is complete
// MEMBER 4: load and display project data from storage
// We are gonna use static project values to add people (or if you disagree just say)
document.addEventListener("DOMContentLoaded", () => {
    // MEMBER 4:
    // Retrieve projects from storage and populate #projectsTableBody
});